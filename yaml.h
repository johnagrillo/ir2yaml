
#ifndef __YAML_H__
#define __YAML_H__

/*
template <typename T> std::string toString(T *t) {
  std::string type_str;
  llvm::raw_string_ostream rso(type_str);
  t->print(rso);
  return rso.str();
}

template <> std::string toString<llvm::Type>(llvm::Type *t) {
  std::string type_str;
  llvm::raw_string_ostream rso(type_str);
  t->print(rso);
  return rso.str();
}

template <typename T>
std::string toStringOperand(T *t, bool printWithout = false) {
  std::string type_str;
  llvm::raw_string_ostream rso(type_str);
  t->printAsOperand(rso, printWithout);
  return rso.str();
}
*/

#endif
